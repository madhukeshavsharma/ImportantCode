<!--

//Install typescript global:
npm install -g typescript@3.3
===================

Check the version: 
tsc --version(Check the version)
====================

For new project is following step:
1. npx create-react-app my-app --template typescript
2. npm install --save typescript @types/node @types/react @types/react-dom @types/jest
3. npm run dev
4.npm run build
5. npm run start
====================

For existing projects is following step:
2. npm install --save typescript @types/node @types/react @types/react-dom @types/jest
3. npm run dev
4.npm run build
5. npm run start
====================


//For vertual server
1. npm install -g serve
2. serve -s build
